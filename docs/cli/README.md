# CLI Tool

The `cli` crate provides a command-line interface for managing Rustenium projects
and running tests.

## Commands

- `init`: Initialize a new Rustenium project.
- `run`: Run tests.
- `report`: Generate and view test reports.

## Installation

```bash
cargo install rustenium-cli
```
